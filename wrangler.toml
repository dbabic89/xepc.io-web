name = "xepcio-web"
compatibility_date = "2026-07-01"

# Only dist/ is uploaded — build.sh assembles it, so .git and repo-only files
# are never part of the deployment.
[assets]
directory = "./dist"
not_found_handling = "404-page"
